Ogni design pattern creazionale è in un package. Avviare sempre dalla classe Main oppure dalla classe Client.

Singleton: per questo design pattern ho creato una classe singleton che ha questa stringa che stampa
Il design pattern singleton l'ho usato in molti esempi dei design pattern strutturali.

FactoryMethod: abbiamo un costruttore virtuale Factory e le sue sottoclassi FactoryRettangolo e FactoryQuadrato
che ci restituiscono una figura geometrica diversa.

AbstractFactory: nell'abstract factory si marca l'appartenenza di una famiglia di prodotti, dalle sue
sottoclassi CostruttoreQuadrato e CostruttoreRettangolo.

Builder: In questo esempio il builder assembla i vari oggetti che compongono senza che il client sappia come avviene.

Prototype: Mi creo le figure e ogni volta che mi viene richiesta dal client restituisco l'oggetto clonato.